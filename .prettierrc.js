/**
 * @type {import('prettier').Config}
 */
const config = {
	tabWidth: 4,
	useTabs: true,
	printWidth: 120,
	singleQuote: true,
	tailingComma: 'es5',
	arrowParens: 'always',
	semi: true,
};

module.exports = config;
